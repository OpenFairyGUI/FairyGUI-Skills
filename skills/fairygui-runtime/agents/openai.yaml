interface:
  display_name: "FairyGUI Runtime (Archived)"
  short_description: "Archived reference for FairyGUI runtime integration"
  default_prompt: "Use $fairygui-runtime as archived reference for FairyGUI runtime code, checking APIs and lifecycle behavior against my installed SDK."

policy:
  allow_implicit_invocation: true
